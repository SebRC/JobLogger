import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#8dc92b",
    },
    secondary: {
      main: "#00695c",
    },
    background: {
      default: "#ffffff",
      paper: "#e8e8e8",
    },
  },
});

export default theme;
