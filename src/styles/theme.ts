import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#23242b",
    },
    secondary: {
      main: "#bd90f5",
    },
    background: {
      default: "#ffffff",
      paper: "#e0f7fa",
    },
  },
});

export default theme;
